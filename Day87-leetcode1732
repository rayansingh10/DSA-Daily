https://leetcode.com/problems/find-the-highest-altitude/

class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int ans=0;
        int maxi=0;
        for(int i:gain){
            ans=ans+i;
            maxi=max(maxi,ans);
        }
        return maxi;
    }
};
